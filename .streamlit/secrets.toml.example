# Copy this file to secrets.toml and fill in your key to enable the Claude
# copilot, insight cards, and briefings. Without a key the app runs fully in
# deterministic rules mode.
ANTHROPIC_API_KEY = ""

# Optional: override the model (defaults to claude-haiku-4-5).
# AGILE_AI_MODEL = "claude-sonnet-4-6"
